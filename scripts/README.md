# Scripts

Store helper scripts here (PowerShell/Python). Examples to add:

- transpile.ps1 / transpile.py: Write -> C/C++ pipeline
- build-gui.ps1: package GUI with PyInstaller
- run-tests.ps1: execute unit/integration test suite
